{
  "name": "posta-kodu-rehberi",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run generate:sitemap && NODE_OPTIONS=--max-old-space-size=4096 nuxi build",
    "dev": "npm run generate:sitemap && nuxi dev",
    "generate": "npm run generate:sitemap && NODE_OPTIONS=--max-old-space-size=8192 nuxi generate",
    "preview": "nuxi preview",
    "postinstall": "nuxi prepare",
    "generate:sitemap": "node scripts/generate-sitemap.js"
  },
  "dependencies": {
    "nuxt": "^4.0.0",
    "lucide-vue-next": "^0.474.0"
  }
}