{
  "name": "posta-kodu-rehberi",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run generate:sitemap && npm run generate:data-collection && nuxt build",
    "dev": "npm run generate:sitemap && nuxt dev",
    "generate": "npm run generate:sitemap && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "generate:data-collection": "node scripts/generate-data-collection.js"
  },
  "dependencies": {
    "@fontsource/inter": "^5.0.0",
    "@fontsource/jetbrains-mono": "^5.0.0",
    "nuxt": "^3.15.4",
    "lucide-vue-next": "^0.474.0"
  }
}